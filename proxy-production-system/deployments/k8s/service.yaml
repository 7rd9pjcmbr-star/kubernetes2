apiVersion: v1
kind: Service
metadata:
  name: proxy-system
  labels:
    app: proxy-system
spec:
  selector:
    app: proxy-system
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
