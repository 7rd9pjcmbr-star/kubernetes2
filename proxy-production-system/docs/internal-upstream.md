# Cấu hình proxy nội bộ

Profile nội bộ đã thu hẹp thành **proxy logistics (J&T)**.

Xem tài liệu chính:

- `docs/logistics-proxy.md`
- `.env.logistics.example`

```bash
cp .env.logistics.example .env
./scripts/reconnect-internal.sh
```
